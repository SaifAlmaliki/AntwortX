/**
 * Client-facing levers to improve brand visibility in AI-generated answers.
 * Keep factual and non-hype; aligns with GEO practice.
 */
export const LLM_VISIBILITY_PLAYBOOK_LINES: string[] = [
  "Clarify your entity: consistent brand name, domain, and Organization schema (including sameAs where relevant) so models can map you to a single thing.",
  "Make key pages crawlable without relying only on JavaScript: server-render or prerender marketing and product copy so audits and crawlers see real text.",
  "Publish citable content: clear H1–H3 hierarchy, short answers to buyer questions, lists and specifics models can quote.",
  "Earn presence where models train and browse: accurate listings and profiles on reputable sites in your category (industry directories, review platforms, etc.).",
  "Use structured data (JSON-LD) for organization, products, or services where it matches what you actually offer.",
  "Optional: add llms.txt (and keep robots.txt aligned) so AI-oriented crawlers understand what you want summarized or linked.",
  "Expect change: rankings and answers shift by product and date — treat this report as a snapshot and iterate.",
];
